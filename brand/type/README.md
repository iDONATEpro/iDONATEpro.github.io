# Type

| Role | Family | Weights | CSS |
|---|---|---|---|
| Display | Archivo | 500, 600, 700, 800 | `--font-display` |
| Body | Inter | 400, 500, 600, 700 | `--font-body` |

Load via Google Fonts (public site already does) or self-host licensed files later.

Headlines: tight tracking (−0.03em style on the marketing site). Body: Inter at 400/500 for UI density.
